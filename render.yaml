services:
  - type: web
    name: grun
    runtime: docker
    # Docker-based deployment forces Node.js 20 via Dockerfile.
    # This bypasses Render's default Bun runtime which caused
    # Next.js 16 Turbopack build failures.
    envVars:
      - key: PORT
        value: "10000"
    plan: free
